import type { LotFetchResult, LotFetcher, LotQuery } from './types.js';

/**
 * Manheim.
 *
 * Площадка закрыта: страницы лотов доступны только зарегистрированным
 * дилерам, публичной версии нет. Обходить эту авторизацию мы не будем —
 * ни кодом, ни иначе.
 *
 * Адаптер существует, чтобы Manheim можно было выбрать в тарифах, расчётах
 * и витрине, и чтобы на кнопку «Подтянуть данные» приходил внятный ответ,
 * а не молчание. Появится партнёрский доступ — здесь меняется одна функция.
 */
export const manheimFetcher: LotFetcher = {
  platform: 'manheim',

  async fetch(_query: LotQuery): Promise<LotFetchResult> {
    return {
      ok: false,
      reason:
        'Manheim не публикует страницы лотов — данные заполняются вручную ' +
        'или через партнёрский доступ дилера',
    };
  },
};
