{
  "name": "avtoklyuch",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "docker compose up --build",
    "down": "docker compose down",
    "logs": "docker compose logs -f",
    "build": "npm run build -w @avtoklyuch/shared && npm run build -w @avtoklyuch/api && npm run build -w @avtoklyuch/app && npm run build -w @avtoklyuch/site-autokey && npm run build -w @avtoklyuch/site-westauto",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
